# Turnos Pro Frontend

Vite + React + Tailwind para la interfaz de Turnos Pro. Incluye layout base, flujo de auth y pantallas principales.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

La API se asume disponible en `http://localhost:5000/api` (configurable en `src/api/axios.js`).
